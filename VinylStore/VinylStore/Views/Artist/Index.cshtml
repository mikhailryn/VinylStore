@model IEnumerable<Artist>

<br />
    <div class="row">

        @foreach (var item in Model)
        {

            <div class="col-lg-4 col-md-6 mb-4">


                <div class="thumbnail">

                    <div class="gallery2">
                    <div class="ramka"> <img src="@item.ImgUrl"/> </div>
                    </div>

                    <div class="caption">
                        @*<h3 class="pull-right">@item.ArtistName</h3>*@
                        <h3>
                            <a asp-controller="Artist" asp-action="Details" asp-route-id="@item.ArtistId">@item.ArtistName</a>
                        </h3>
                        <p>@item.Description</p>
                    </div>

                    @*<div>
                        @Model.Album.Artist.ArtistName

                    </div>*@

                    @*<div class="addToCart">
                        <p class="button">
                            <a class="btn btn-primary"
                               asp-controller="ShoppingCart"
                               asp-action="AddToShoppingCart"
                               asp-route-vinylId="@Model.VinylId">Add to cart</a>
                        </p>
                    </div>*@


                </div>


            </div>

        }



    </div>